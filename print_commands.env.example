# Copy this file to instance/print_commands.env and edit printer names.
# The app loads this file automatically at startup.
#
# One-time setup:
# 1) Install CLI: python -m pip install bambu-cli
# 2) Add printers: python -m bambucli.cli add-local
# 3) List info:    python -m bambucli.cli info "YOUR_PRINTER_NAME"
#
# Use the exact printer names saved by add-local.
# {file} = full local path to uploaded file in instance/gcode_uploads
# {filename} = original filename
# {job_id} = db id

ASME_H2S_PRINT_CMD=python -m bambucli.cli print "H2S_PRINTER_NAME" "{file}"
ASME_P1S_PRINT_CMD=python -m bambucli.cli print "P1S_PRINTER_NAME" "{file}"

# -----------------------------
# Google Calendar integration
# -----------------------------
# Path to Google service account JSON key (share your calendar with this service account email).
ASME_GCAL_SERVICE_ACCOUNT_FILE=instance/google_service_account.json

# Calendar IDs:
# - You can set one default ID, or separate IDs per room.
ASME_GOOGLE_CALENDAR_ID=
ASME_GOOGLE_CALENDAR_ROBOTICS_ID=
ASME_GOOGLE_CALENDAR_FLUIDS_ID=
ASME_GOOGLE_CALENDAR_TZ=America/Chicago

# Optional embed override for the iframe:
# ASME_GOOGLE_CALENDAR_EMBED_URL=

# -----------------------------
# Gmail cancellation approvals
# -----------------------------
# Use an App Password for Gmail SMTP (not your normal account password).
ASME_SMTP_HOST=smtp.gmail.com
ASME_SMTP_PORT=587
ASME_SMTP_USER=uiowaasme@gmail.com
ASME_SMTP_PASS=
ASME_CANCEL_NOTIFY_TO=uiowaasme@gmail.com
