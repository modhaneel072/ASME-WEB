# Copy this file to instance/print_commands.env and edit printer names.
# The app loads this file automatically at startup.
#
# One-time setup:
# 1) Install CLI: python -m pip install bambu-cli
# 2) Add printers: python -m bambucli.cli add-local
# 3) List info:    python -m bambucli.cli info "YOUR_PRINTER_NAME"
#
# Use the exact printer names saved by add-local.
# {file} = full local path to uploaded file in instance/gcode_uploads
# {filename} = original filename
# {job_id} = db id

ASME_H2S_PRINT_CMD=python -m bambucli.cli print "H2S_PRINTER_NAME" "{file}"
ASME_P1S_PRINT_CMD=python -m bambucli.cli print "P1S_PRINTER_NAME" "{file}"

# -----------------------------
# Outlook Calendar integration
# -----------------------------
# Azure AD app (client credentials flow) for Microsoft Graph Calendar API.
ASME_OUTLOOK_TENANT_ID=
ASME_OUTLOOK_CLIENT_ID=
ASME_OUTLOOK_CLIENT_SECRET=

# Mailbox user to create/delete events under.
# Must be a Microsoft 365 mailbox in your Entra tenant, not Gmail/Yahoo/iCloud.
ASME_OUTLOOK_CALENDAR_USER=asme@yourtenant.edu

# Calendar IDs:
# - You can set one default ID, or separate IDs per room.
# - If left blank, events are created on the mailbox default calendar.
ASME_OUTLOOK_CALENDAR_ID=
ASME_OUTLOOK_CALENDAR_ROBOTICS_ID=
ASME_OUTLOOK_CALENDAR_FLUIDS_ID=
ASME_OUTLOOK_CALENDAR_TZ=Central Standard Time

# Optional Outlook embed URL for the iframe:
# ASME_OUTLOOK_CALENDAR_EMBED_URL=

# -----------------------------
# Cancellation approval emails
# -----------------------------
# Office 365 SMTP settings (or replace with your provider).
ASME_SMTP_HOST=smtp.office365.com
ASME_SMTP_PORT=587
ASME_SMTP_USER=asme@yourtenant.edu
ASME_SMTP_PASS=
ASME_CANCEL_NOTIFY_TO=asme@yourtenant.edu
